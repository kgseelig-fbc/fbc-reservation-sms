# ─────────────────────────────────────────────────────────────────
#  Reservation SMS Server — Environment Variables (multi-tenant)
#  Copy this file to .env and fill in your values
# ─────────────────────────────────────────────────────────────────

# Server
PORT=3001
NODE_ENV=production

# Postgres connection string (Railway injects this when you add a Postgres plugin)
DATABASE_URL=postgres://user:pass@localhost:5432/fbc_sms

# Session secret (random hex). If unset, a new one is generated on each boot,
# which invalidates existing sessions on every restart.
SESSION_SECRET=

# Comma-separated list of allowed CORS origins (leave blank for same-origin only)
CORS_ORIGINS=

# ── Twilio credentials ─────────────────────────────────────────────
# NOTE: Twilio credentials are now stored PER FRANCHISE in the database.
# The env vars below are only used to seed the default franchise ("FBC NE
# Florida") on first migration. You can remove them after the first deploy
# once the franchise row has its own credentials in the DB.
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_MESSAGING_SERVICE_SID=

# Public URL (used for Twilio webhook status callbacks, seeded onto the
# default franchise). New franchises set their own base_url via the CLI.
BASE_URL=https://your-domain.com

# ── Google OAuth (sign-in) ────────────────────────────────────────
# Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must match GOOGLE_OAUTH_REDIRECT_URL exactly,
# e.g. https://your-domain.com/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional override. If unset, the server derives this from the request host,
# which works for most setups behind a proxy (trust proxy is enabled).
GOOGLE_OAUTH_REDIRECT_URL=
