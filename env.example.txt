# ─────────────────────────────────────────────────────────────────
#  Twilio SMS Confirmation Server — Environment Variables
#  Copy this file to .env and fill in your values
# ─────────────────────────────────────────────────────────────────

# Server
PORT=3001

# Your public URL (needed for Twilio webhooks)
# In development, use ngrok: https://ngrok.com
# Example: BASE_URL=https://abc123.ngrok.io
# In production: BASE_URL=https://yourdomain.com
BASE_URL=https://your-domain.com

# Twilio Credentials
# Find these at https://console.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here

# Your Twilio phone number (must be SMS-capable)
# Buy one at https://console.twilio.com/us1/develop/phone-numbers
# Format: +1XXXXXXXXXX
TWILIO_PHONE_NUMBER=+1234567890

# Postgres connection string
# Railway provides this automatically when a Postgres plugin is attached
# Local example: postgres://user:pass@localhost:5432/fbc_sms
DATABASE_URL=postgres://user:pass@localhost:5432/fbc_sms

# Admin password for dashboard login
ADMIN_PASSWORD=change-me

# Session secret (random hex; if unset, a new one is generated on each boot,
# which invalidates existing sessions on restart)
SESSION_SECRET=

# Comma-separated list of allowed CORS origins (leave blank for same-origin only)
CORS_ORIGINS=
